# dashboard (placeholder)

Phase 1 の管理ダッシュボードはこのディレクトリに実装する（本セッションでは未着手）。

- 顧客レコード作成・一覧（RLS: 担当営業は自分の顧客のみ）
- タグID登録フォーム → LPShell 自動注入
- 公開/非公開切り替え → Vercel Deploy Hook 発火
- commitad_client_id 紐づけ入力

実装は `docs/lp-platform-spec.md` セクション8 Phase 1 を参照。
