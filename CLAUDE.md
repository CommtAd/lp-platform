# CLAUDE.md — LP制作規約（営業向け）

このリポジトリで新しいLPを制作・修正するときは、以下の規約に必ず従うこと。
基盤の責務（公開・タグ注入・CV受信と転送）を壊さないための最小限のルール。

## 基本ルール（lp-platform-spec.md §9）

1. LPは `apps/lp/clients/{slug}/` 配下に作成する。
   ベーステンプレート（`clients/_base-a/`）をコピーして開始する。
2. **LPShell で全体をラップすること（削除・迂回禁止）。**
   タグ注入・noindex・UTM保持はすべて LPShell が担う。外すと機能が失われる。
3. **フォームは LPForm を使用する。送信先URLの変更禁止。**
   送信先は基盤側で固定（`lib/form-endpoint.ts`）。HTML納品時も動作させるため。
   フィールド構成・見た目は `config.ts` の `form` で自由にカスタマイズしてよい。
4. `tel:` リンク・LINEリンクには `trackEvent('tel_tap' | 'line_tap', slug)` を付与する。
5. それ以外のデザイン・構成・アニメーションは自由。
6. 画像は `clients/{slug}/` 配下に置き、`config.ts` からパス参照する
   （`public/clients/{slug}/` に配置し、`ImageSlot` で aspect-ratio + object-fit: cover 受け）。
7. 公開フロー: PR作成 → プレビューURLで顧客確認 → main マージ →
   ダッシュボードで公開ボタン（status を published に切り替え）。

## 制作フローの追加ルール（implementation-brief-01.md §6）

8. **新規LP制作時は必ず該当 brief.md を読み、`design-refs/`（参照画像）を確認してから着手する。**
9. **Claude Design の出力コードを直接持ち込まない。**
   ベーステンプレート（`_base-*`）からの差分適用で制作する。
10. **コード化後のデザイン修正はコード側で行う。Claude Design には戻らない（一方通行ルール）。**
11. **参照画像のデザインは参考にしてよいが、画像素材そのものをLPに流用しない。**
    参照画像は見た目の検収基準。実際の画像は顧客素材またはプレースホルダを使う。
12. **フォルダ名（slug）はダッシュボードに登録済みの slug をそのままコピーする。**
    ダッシュボードで枠を作る → その slug でLPフォルダを作る、が正規の順序。
13. **PRを出す前に `npm run check-slug-sync` を実行し、必ずパスさせる。**
    フォルダ名と `clients.slug` がずれると、ページは200で表示されるのに
    タグが一切注入されないという気づきにくい壊れ方をする（実際に2件発生）。
    落ちた場合はダッシュボード側の slug を修正するマイグレーションを追加する
    （`supabase/migrations/0013`, `0014` が実例）。
14. **業種を分けたくなっても、リポジトリ・Vercelプロジェクト・LPShell・LPForm は分けない。**
    基盤は業種非依存に作ってある。分けるのはテンプレ・公開ドメイン・
    ダッシュボードの `clients.industry` の3レイヤーだけ（`docs/implementation-brief-02.md` §0）。
15. **ダッシュボードで枠を作るとき、業種（`industry`）を必ず正しく選ぶ。**
    一覧のタブ分けにのみ使う値で、LPの表示・タグ注入には影響しない。

## テンプレA（`_base-a`）と実顧客LPの関係

- `_base-a/` = パターンAの原型テンプレ。テキストはダミー、画像はプレースホルダ。
  **テンプレA自体の改善はこのフォルダで行う。**
- `the-personal-pilates/` = 実顧客LP。実データで運用する本番。
- `page.tsx` / `FaqList.tsx` は両者で同一のコピー。差分は `config.ts` のみ。
  新規顧客は `_base-a/` をコピーし、`config.ts` を実データに差し替える。
  **例外: 横幅ずれ防止ルール（§16〜20）由来の差分は既存LPへコピーしない。**
  テンプレは `<LPCanvas>` 化済みだが、既存LPは適用対象外のため意図的に差分がある。

## 保持すべき確定デザイン要素（パターンA・変更禁止）

- 配色: ネイビー × ゴールド × 生成り。見出しは明朝、本文はゴシック。
- セクション構成（順序固定）:
  ヘッダー（オファーバー）→ FV（縦書きキャッチ）→ 90分お試し体験（0円 + 6項目の円形アイコン）
  → 入会金0円 → 料金 → スタジオについて → 悩み共感4枚 → 選ばれる3つの理由
  → 通い方シーン3例 → トレーナー紹介（スワイプ）→ 90分体験の流れ3ステップ
  → FAQ4問 → 店舗案内2店 → 予約フォーム。
- 「業界最安クラス」は削除済み。復活させない。
- 二重価格表記（11,000円→0円 / 33,000円→0円）は顧客確認済み。そのまま維持。

## 保持すべき確定デザイン要素（パターンC・ブライダルフェア・変更禁止）

テンプレは `_base-c/`、型は `clients/pattern-c.types.ts`、指示書は `docs/implementation-brief-02.md`。
パターンA／Bとは独立したデザインシステム。**混ぜない**。

- 配色: 生成り `#FBF8F3` × チャコール `#3B3730` × シャンパンゴールド `#B99653`。
  見出しは明朝（Shippori Mincho）、英字キッカーは Playfair Display のイタリック、本文はゴシック。
- 特典バンド（限定特典・来館特典）の地色は `config.band` で顧客ごとに変えてよい。
  ただし**ゴールドは中間トーンなので中間色の地に載せると文字が消える**。
  地を明るくしたら `band.accent` / `band.rule` を濃色に振る。
- **金額は必ず白プレート＋深い金 `#8C6B2F` で置く（バンドの配色に関係なく固定）。**
  ブランドゴールド `#B99653` は白地で2.6:1しか出ないので、白地では深い金を使う。
  地に直接白文字で流すと、一番見せたい桁が一番弱い要素になる（実際に発生）。
  数字部分は `amountEmphasis()` が自動で特大にする。
- FVは写真全面＋下方向グラデーション。パターンAの縦書きキャッチは使わない。
- セクション構成（順序固定）:
  ヘッダー → FV → ブランド紹介 → 限定特典（成約特典）→ 開催日程 → 施設紹介（カルーセル）
  → 体験できること（カルーセル）→ おすすめな方 → 来館特典（金額＋TOTAL）
  → 会場ギャラリー → 選ばれる理由 → プラン例 → 先輩カップルの声 → 当日の流れ
  → FAQ → アクセス → 予約概要 → 予約フォーム。
  **順序は固定だが、`pattern-c.types.ts` で `?` の付いたセクションは丸ごと省略してよい。**
  式場ごとに出せる情報が違うため、空欄で埋めるより落とすほうが仕上がりが良い。
  省略時は `page.tsx` が自動でスキップする（条件分岐を書き足す必要はない）。
- CVは「フェア来館予約」。フォームは**来館希望日を必ず取る**。
  時間帯・挙式希望時期・人数・試食の有無は案件の原稿に従う。
- 特典金額・プラン価格・二重価格（`was`）は顧客確認を取ってから記載する。
- 電話番号は `_base-c/TelLink.tsx` 経由で置く（`trackEvent('tel_tap')` が自動で付く）。
- 公開ドメインは `bridal-lp.commitad.com`（同一Vercelプロジェクトにドメイン追加のみ）。
- 実顧客の1件目は `forest-terrace-hiroshima/`（ザ・フォレストテラス広島）。
  `page.tsx` / `FaqAccordion.tsx` / `TelLink.tsx` は `_base-c/` と同一コピー。差分は `config.ts` のみ。
  テンプレ側を直したら**必ず全パターンCのLPへコピーし直す**こと。
  **ただし横幅ずれ防止ルール（§16〜20）由来の差分＝`<LPCanvas>` 化と
  `vw` 撤去はコピーしない。** 既存LPは適用対象外なので、この差分は意図的に残す。

## 横幅ずれ防止ルール（2026-08-21以降の新規LPのみ・変更禁止）

**適用範囲: これから作る新規LPだけ。既存LPには適用しない。**
ルール制定（2026-08-21）時点で存在していた17本は、顧客確認済みの見た目を
こちら都合で動かさないため現状のまま据え置く。**既存LPを移行しないこと。**
対象外リストは `scripts/check-responsive.ts` の `EXISTING_BEFORE_RULE`。
このリストは増えも減りもしない（新規を足すのも、既存を消すのも禁止）。

16. **LPは「幅390pxの1枚のキャンバス」として作る。キャンバスは `<LPCanvas>` で組む。**
    `apps/lp/components/LPCanvas.tsx` / `globals.css` の `.lp-canvas` が実体。
    中身は常に設計幅390pxでレイアウトされ、表示幅に応じて**キャンバスごと**
    拡大縮小される（実寸は `min(ビューポート幅, 480px)`、PCでは1.23倍表示）。
    したがって360pxの実機でも1280pxのPCでも、改行位置・要素の比率・高さが一致する。
    **PCで確認した見た目がそのまま実機の見た目になる**のがこのルールの目的。

    自前で `maxWidth: 480` + `margin: 0 auto` のキャンバスを組むのは禁止。
    それは「可変幅のキャンバス＋固定pxの中身」であり、幅が変わると中身だけが
    取り残されて崩れる（実測: `the-personal-pilates` を480pxと360pxで比較したところ、
    542要素中390要素の比率がずれ、15箇所で改行数が変わった）。

17. **キャンバスの内側でビューポート単位（`vw` / `vh` / `dvh` など）を使わない。**
    ビューポート単位は zoom の外側＝ウィンドウ基準で解決されるため、
    同じ指定がPCでは1280px、実機では390pxを指す。
    パターンCの `calc((100vw - 42px) / N)` による自動フィットが実際にこれで、
    PCで13pxに見えていた文字が実機では9.6pxになっていた。
    設計幅は常に390pxで固定なので、`DESIGN_WIDTH` を使った素の割り算に置き換える
    （`LPCanvas` から `DESIGN_WIDTH` を import する）。
    例外はキャンバス**外**の地色を敷く `minHeight: "100vh"` のみ。

18. **はみ出しを切るときは `overflow: hidden` ではなく `overflow-x: clip` を使う。**
    `hidden` はスクロールコンテナを作るため、中の `position: sticky` ヘッダーが
    ビューポートに固定されなくなる（パターンB/Cのヘッダーが実際に効いていなかった）。
    `.lp-canvas` は `clip` を使っているので、`<LPCanvas>` を使えば自動的に正しい。

19. **PRを出す前に `npm run check-responsive` を実行し、必ずパスさせる。**
    16・17に違反していないかを機械的に確認する。CIの `check` にも入っている。
    検査対象は `EXISTING_BEFORE_RULE` に載っていないLP＝新規LPだけ。
    **新規LPをこのリストに足して逃げるのは禁止**（それをやるとルールが無効になる）。

20. **新規LPの検収は「PC幅」と「360px幅」の2つで行い、改行位置が一致することを確認する。**
    一致しなければ16か17に違反している。ブラウザの開発者ツールで幅360pxにして、
    PC表示と改行が1文字もずれないのが正しい状態。
    既存LPはこの基準の対象外（元々ずれる作りなので、比べても意味がない）。

## チーム共同運用フロー（修正→公開・追加費用なし）

複数メンバーが各自のClaude Codeで修正〜公開まで行う前提の運用ルール。
**main への直接pushは禁止（ブランチ保護済み）。必ずPR経由で、CIが通ってからマージする。**

1. **ブランチを切る**: `git switch -c fix/<内容>`（mainで直接作業しない）。
2. **修正する**: Claude Codeで編集。ローカルで `npm run build` が通ることを確認。
3. **PRを作る**: `git push -u origin <branch>` → `gh pr create`。
   push先がmainでないこと、PR差分が意図通りかを必ず確認する。
4. **CIの通過を待つ**: GitHub Actions の `check`（check-rules + build）が緑になること。
   赤なら本番に反映されない仕組み。ログを見て直し、pushし直す。
   完了待ちは `gh pr checks <PR番号> --watch --fail-fast` を使う（完了で自動終了。`check` は通常40秒前後）。
   自作の待ちループは避ける。特に **macOS の grep は `-P`（Perl正規表現）非対応**で、
   待ちループに `grep -P` を書くと毎回無言で失敗し、判定不能のまま延々と待ち続ける
   （「CIが遅い」の誤診の原因になった実例あり）。ワンショット確認は `gh pr view <PR番号> --json mergeStateStatus`（CLEAN=通過）。
5. **マージする**: CIが緑になったら `gh pr merge --squash`。mainへ反映される。
6. **公開する**: ダッシュボードの公開ボタンで status を published に切り替える
   （Vercel Deploy Hook が発火 → 本番反映）。Vercelの席は不要＝追加費用なし。
7. **本番を確認する**: 本番LPは独自ドメイン **`https://fitness-lp.commitad.com`** で配信される。
   `lp-platform-lp.vercel.app` は 308 でこのドメインへリダイレクトするため、
   確認は独自ドメインを直接開く（curlで検証する場合は必ず `-L` を付ける）。
   リダイレクト追従なしで vercel.app を叩くと本文が空になり「未反映」と誤検知するので注意。

- **権限**: メンバーはGitHubの collaborator（write）とダッシュボードのSupabase Authユーザーを付与すれば足りる。いずれも無料枠。
- **保護設定**: `check` 通過必須・PR必須・force-push/削除禁止。管理者のみ緊急時にバイパス可。
  ルールを緩めたい場合もコードや設定側で調整し、保護自体は外さない。
